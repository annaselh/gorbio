package crm

// Permission codes owned by this module. Declaring them keeps the strings that
// guard the routes in one place instead of scattered literals.
const (
	PermissionRead   = "crm.read"
	PermissionManage = "crm.manage"
)
